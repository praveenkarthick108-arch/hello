"""
Evaluation package for the trip planner.

Suites:
  basic_evals   — fast, deterministic checks on state structure and arithmetic
  ragas_evals   — RAGAS faithfulness + answer relevancy on the RAG pipeline outputs
  deepeval_evals — DeepEval answer relevancy, faithfulness, hallucination, and GEval
  eval_runner   — orchestrates all suites and produces a combined report
"""
